# Close-out Phase 0 — close by decision

**Parent:** `docs/plans/2026-09-26-close-out-plan.md`, Phase 0.

**Goal:** the `open-items.md` rows that need only a decision are closed or re-pointed, in one docs commit.
No code, no gates beyond a grep.

**Status:** done 2026-09-26. The step-4 wording quoted below was corrected in the skill the same day (it
blamed `db99d93`, which is the fix, and called a `v*` tag unmovable).

---

## Decisions (the user, 2026-09-26)

| # | Question | Answer |
|---|---|---|
| D1 | §I rows: which close as won't-fix? | **Only the `Menu.tsx` ceiling.** Every other row stays open: S1, S2, S3, S4, B3, C6/Q23, F3, R10, R12, the `App.tsx` race and the `log/walker.rs` ceiling go to Phase 2; E6 and R13 (performance) to Phase 3 |
| D2 | §C roadmap (custom titlebar, i18n, plugins) | **Keep open.** No change to §C |
| D3a | Windows code signing | **Not closed: ported** from `F:/src/_ pet projects/signing-and-repo-setup.md` (Certum, a `signing` environment, pinned actions) as close-out **Phase 1b**, right after Phase 1 |
| D3b | macOS notarization | **Won't do for now.** Closed with its mitigation and a reopen condition |

## Step 1 — the four record-only smoke boxes

There is no marker for "closed, not walked" in the smoke docs; ticking them would claim a walk. Use
`- [n/a]` plus a dated reason, keeping the existing text. Lines 1742 and 1854 are numbered (`9. - [ ]`), so
there the marker is `9. - [n/a]`. The edit changes no line counts, so every other line ref stays valid:

| Line (`smoke-test-post-v1.md`) | Box | Reason written |
|---|---|---|
| 1152 | AG — a late status does not take you out of the working-tree row | recipe falsified, no achievable one |
| 1163 | AG — no spurious re-walk as the state changes | what it observes cannot decide it |
| 1742 | viewport-anchor 9 — scroll during the restart | not drivable; covered by `repoStore.test.ts` |
| 1854 | AZ 9 — a skipped path says so | no hand recipe; covered by `check_staged` |

Then in `open-items.md`:
- §B: drop the *Records, not work (4)* sub-list, recount the header to **six** (3 by hand, 3 on Linux /
  macOS), fix the stale refs `:1855`, `:1856` → `:1857`, `:1858`.
- §M *Open boxes in group AZ*: 9 is closed, only 11 remains.

## Step 2 — §H into the release skill

A universal rule rather than "if anything touched `#[cfg(unix)]`", which has no cheap check. In
`.claude/skills/release/SKILL.md`, split Steps › 4 so it reads:

> 4. **Push `main`, wait for its CI run, then push the tag.** Push `main` first: the tag's run checks out the
>    commit, and a tag that arrives before its commit points at nothing on the remote. Then wait for the CI run
>    of that push to go green on all three OS (`gh run watch`). The local gate runs on Windows only, and Windows
>    clippy never compiles `#[cfg(unix)]` code — `db99d93` went red on Linux and macOS after a green local gate.
>    A red leg on the tag's own `checks` would burn the version number, since a `v*` tag cannot be moved.

The code block keeps its three commands, with the wait between the push and the tag. About 15 minutes per
release.

Move the §H row to `open-items-done.md` §H: *became release-skill step 4 (tag only after `main`'s CI is
green on all three OS), 2026-09-26*. §H is then empty in
`open-items.md` — delete the heading.

## Step 3 — §I (per D1)

- Move the `Menu.tsx` ceiling to `open-items-done.md` §I (*the rows since closed*): **closed 2026-09-26, will
  not fix** — a submenu panel is `.menu`-wide, so the parent's width stands in. Leave the `ponytail:` comment
  in `Menu.tsx`; it still names the ceiling. Reopen if a submenu's labels clip.
- Every other §I row: append *(close-out Phase 2)*, or *(close-out Phase 3, measure first)* on E6 and R13.
- Add the seven `ponytail:` ceilings in code that §I never listed (user, 2026-09-26: track them), to the
  ceilings sub-list, each with its phase:
  - `crates/git-core/src/linked.rs:120` — `snapshot` opens a repository per worktree and re-reads every
    submodule, no cache *(Phase 3, measure first)*;
  - `crates/git-core/src/linked.rs:134` — no main row when the main worktree's HEAD can't be read;
    `worktree list --porcelain` fixes it at a git ≥ 2.36 floor *(Phase 2)*;
  - `crates/git-core/src/watch.rs:124` — an app-side rewrite of `.gitmodules` does not refresh the Submodules
    list until the next refs event *(Phase 2)*;
  - `src-tauri/src/commands/window.rs:326` — the pointer position for tab adoption is Windows-only *(Phase 5)*;
  - `src/components/ui/Input/Input.tsx:221` — an AltGr character never reaches type-ahead *(Phase 2)*;
  - `src/screens/RepoWindow/Toolbar.tsx:100` — a rename while in the `icons` tier measures late *(Phase 2)*;
  - `src/screens/RepoWindow/dialogs/StashDialogs.tsx:39` — a dirty-only submodule is listed as stashed
    *(Phase 2)*.
- Reword the §I intro, which says each row is "deliberately not fixed; the reason is the condition that
  reopens it" — no longer true. New intro: *Deferred findings lifted from
  `docs/archive/plans/2026-09-12-consolidated-findings.md` and later reviews, plus the `ponytail:` ceilings in
  code. Each was low and deferred with a reason; since 2026-09-26 they are scheduled in the close-out plan
  (`2026-09-26-close-out-plan.md`), each row naming its phase.*

## Step 4 — signing rows (per D3)

- **Windows code signing** (§B) stays open; replace its "Fixing it needs…" sentence with a pointer: *Close-out
  Phase 1b ports `signing-and-repo-setup.md` (Certum certificate, thumbprint `F06C…8151`, expires 2027-09-22)
  from t4-markdown-viewer.* Keep the BF 3 observation.
- **macOS notarization** — append to `open-items-done.md` §B: *closed 2026-09-26, won't do for now. Needs a
  paid Apple Developer account; the app is signed with the shared self-signed certificate (stable identity,
  so folder grants survive updates), and the release body carries the quarantine step. Reopen when there is a
  Mac user.* Delete the row from `open-items.md` §B.
- **The user's own 0.10.11 → 0.10.12 update** (§B) — reword. It cannot be the first real run of the
  plain-words errors and Install's confirm: an update runs the *old* app's code, and those shipped in 0.10.12.
  The Phase 1 walk upgrades the user's install to 0.10.12 anyway (the local build's updater installs into the
  same folder). New text: *The first real run of 0.10.12's plain-words update errors and Install's confirm is
  the user's update from 0.10.12 to the next release — the close-out plan's release gate.*

## Step 5 — housekeeping

- README *Status / roadmap* has two stale facts in the same paragraph; fix both:
  - lines 139–141 call "the longer roadmap (bisect, multi-repo tabs, …)" open; both shipped. Change the
    parenthesis to *(custom titlebar, i18n, plugins)*;
  - line 131 says "seven releases (v0.1.0 through **v0.7.0**)". The feature list after it stops at 0.8, so a
    new count would read as complete. Write *releases v0.1.0 through **v0.10.12** (the full list, with notes,
    on GitHub Releases)* — no count.
- `open-items.md` header: the list of letters with nothing open gains §H.
- `open-items.md` *Suggested order*: replace with one line pointing at the close-out plan.
- Close-out plan: Phase 0 status → **done 2026-09-26**, no hash (a commit cannot carry its own). The hash
  goes in with the next commit that touches the close-out plan.

## Step 6 — verify and commit

- `grep '\[ \]' docs/smoke/*.md` → exactly six boxes (+ the prose hit at `post-v1:296`).
- The `Menu.tsx` ceiling and §H's `cfg(unix)` row appear in `open-items-done.md` and not in `open-items.md`;
  notarization likewise.
- Every `ponytail:` comment under `src/`, `crates/`, `src-tauri/src/` is named in `open-items.md` §I, except
  `Menu.tsx`, which is in the done file.
- Two commits, both via `git commit -F -` with a quoted heredoc:
  1. `docs: close-out plan` — the two plan files.
  2. `docs: open-items - close-out phase 0 ...` — Steps 1–5.
- No push (ask first).

**Who:** the main session, directly — small doc edits, no agent.
