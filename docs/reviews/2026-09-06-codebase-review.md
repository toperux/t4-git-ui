# Codebase review — 2026-09-06

Read-through of everything that changed since the previous review's fix commit (`3e723cd`,
"work the 2026-09-03 codebase review") up to `f8b5e94`: the CI trigger and reusable checks
workflow and the release staging fix (`dc7acd4`, `a45b07c`, `c23992d`), `checkout_detach` dropped
and `appIpc.ts` folded into `ipc.ts` (`613f28c`), the nine post-v1 smoke findings (`7b6ef75`), the
v1 re-walk findings (`677e10f`), the six full-rewalk findings and the small UI observations
(`9e0757e`), the merge-resolved-to-HEAD commit and the hunk-button focus (`b15c109`), and the docs
since — the smoke docs as a tick record, `smoke-cdp.md`, `smoke-dialog.ps1`, the two plan files.
Existing code was read where the new code leans on it (`with_index`, `reachers` / `children_first`,
`pending_message`, `Dialog` / `Menu` focus restore, `commitStore`'s prefill and selection sync,
`DiffViewer`'s cursor carry-over, the grid's pseudo-row index math, `release.yml` end to end).

Four independent passes (Rust · stores + IPC + ui components · screens + diff · tests + docs +
workflows), findings merged and de-duplicated here (three rows were found by two passes). Every
row was verified at the cited line; the Rust and git-behaviour rows against git 2.55.0 in
throwaway repos; the screen rows by scratch vitest cases outside the repo (10 assertions). Gates
at `f8b5e94` before any change: vitest 46 files / 394 tests, `cargo test --workspace` 162,
`cargo clippy --workspace --all-targets -D warnings` clean.

Triaged and worked the same day; every row's outcome is in its **Decision** column (all 58 rows
fixed; the two rows whose proposal offered a choice — L18, L24 — say which was taken). Gates after:
vitest 48 files / 417 tests, `cargo test --workspace` 165, clippy and `tsc` clean.

Not read: CSS beyond the rules the findings touch, `docs/design/*`, tests except to list gaps,
`react-resizable-panels` internals (see Unverified).

Severity: **M** = user-visible misbehaviour, data loss, or a smoke-test step that would file a
false bug · **L** = edge case / polish · **P** = performance (no bug) · **H** = hygiene / tests.

---

## M — medium

| # | Where | What | Scenario | Proposed fix | Decision |
|---|---|---|---|---|---|
| M1 | `crates/git-core/src/cli/ops.rs:187` (`checkout`, the only builder left after `613f28c`), `src-tauri/src/commands/ops.rs:278`, callers `Sidebar.tsx:385` (tag menu) and `RefDialogs.tsx:392` (quick checkout) via `actions.ts:45` `checkoutDetached(name)` | The detached path emits a bare `git checkout <target>` — no `--detach`. Both callers pass a **name**, and when a tag and a branch share it git resolves the branch and stays attached. Verified on 2.55: tag `release` at `e820cc4`, branch `release` at `e440573` → `git checkout release` warns `refname 'release' is ambiguous`, switches to the **branch**; `--detach` leaves HEAD detached at the tag. The toast still says "Checked out release (detached)". | Repo with tag `v1.0` and branch `v1.0` that have moved apart; Sidebar → tag → "Checkout (detached)" → the branch is checked out, attached, at its tip; the next commit lands on it. | Give `checkout` a `detach` flag (or restore `checkout_detach` taking a ref name) and pass `--detach` from the detached path; argv test. | fix — `checkout(target, create_branch, track, detach)`; `--detach` only without `-b`; `ipc.checkout` gained a trailing `detach = false`, `checkoutDetached` passes `true`; the `tests/ops.rs` detach case now goes through the builder. The group K walk then found `git checkout --detach <name>` still reads a branch name first: the tag paths (Sidebar, quick checkout) now pass `refs/tags/<name>` via `checkoutTag` |
| M2 | `src/components/ui/Dialog/Dialog.tsx:65` (busy-recovery effect) with `:32` `FOCUSABLE` and `:110` the header Close; consumer `StartScreen/CloneDialog.tsx` | `if (!busy && el && !el.contains(document.activeElement)) el.querySelector(FOCUSABLE)?.focus()` takes the **first focusable in document order**, which is the title bar's ✕ Close — it precedes the body. `CloneDialog` disables every field while `running`, so the focus is on `<body>` when a clone fails and lands on Close. | Clone…, URL with a typo, Clone → fails, error banner, focus on ✕. Enter to retry → the dialog closes; URL, parent folder and name are gone. | Restore into the body (query inside `s.body`), or remember the element that had the focus when `busy` went true. | fix — the busy-recovery effect queries `FOCUSABLE` inside the body first, the form as fallback |
| M3 | `docs/smoke-test.md:190` | Reads "Pull with mode Fast-forward only on diverged history → **non-fast-forward toast**", but `677e10f` split that case into its own `diverged` kind: `opsStore.ts:123-125` → "Cannot fast-forward — the branches have diverged", **no** Pull action; `nonFastForward` (`:118-122`, the push step two lines above) → "remote has new commits — Pull first" **with** Pull. `2026-09-05-full-rewalk.md:86` already logs the new wording. The step was never unticked. | The tester files "the Pull action is missing from the fast-forward-only pull toast". | Reword to the actual toast and say it is a different failure kind (the pull already fetched, so no Pull action). | fix — doc |
| M4 | `src/screens/RepoWindow/RevisionGrid/RevisionGrid.tsx:267` (`{!branches.headCommit && (`) with `:249`, `:272`; `RevisionGrid/commitMenu.ts:63` | The merge item is gated on `headCommit`, not on `merge.length`. On an **unborn HEAD** `refs.head.oid` is `null`, so `headCommit` is false and the item renders as `Merge commit <short> into <current>…`, clicking through to `openDialog({ kind: "merge", branch: oid })`. The previous review's L19 emptied `merge` for `unborn`, but an empty array now only changes the label. `git merge <oid>` on an unborn HEAD **succeeds** (verified: exit 0, the branch moves onto the commit, its tree is checked out). | `git checkout --orphan wip`, stage a file, All-branches walk showing `main`. Right-click any commit → "Merge commit … into HEAD…" → confirm → the orphan branch silently becomes that commit and its tree lands in the working directory. | Hide the item whenever `refs.head.oid === null` — export `unborn` on `CommitBranchActions` beside `canRebase` and gate on it; render-level test. | fix — `unborn` on `CommitBranchActions`; the item is gated on `!headCommit && !unborn` |
| M5 | `src/screens/RepoWindow/CommitPanel/FilesColumn.tsx:151` + `:301`; same pattern `DiffViewer/DiffViewer.tsx:387` + `:410` | `held` is released only by a blur with a `relatedTarget` outside the element. Clicking any **non-focusable** area (a panel header, the diff's path, the panel background) blurs to `<body>` with `relatedTarget === null`, so `held` stays true — and the `[rows]` effect then takes the focus on *any* rebuild. `rows` is rebuilt on every status refresh (`splitStatus(status)` on a fresh `status` object), so a save by any tool in the repo pulls the focus into the list. The comment at `:145-147` promises the opposite. Reproduced in scratch tests for both components. | Click a row, then click the "Unstaged" header text (focus → body). Save any file in your editor → the Unstaged list lights up with a focus ring on its own, and the next Delete / Enter is handled by it. | Only repair a focus the element itself lost: clear `held` on a blur whose target is still connected on the next microtask (a removed row is not), or clear it whenever `document.activeElement` is `<body>` at the start of a refresh. Tests for the release side in both components. | fix — a blur with no `relatedTarget` releases `held` on the next microtask when the blurred node is still connected (a removed row is not); both components. The group K walk found a row button disabled while the operation runs blurs the same way, still connected — that loss stays the list's own |

## L — low

| # | Where | What | Scenario | Proposed fix | Decision |
|---|---|---|---|---|---|
| L1 | `crates/git-core/src/stage.rs:198-209` (`remove_paths`) | The working files are deleted **inside** the `with_index` closure (`:204`), before `index.write()` (`:30`); the rollback at `:32` restores the index only. A held `index.lock` (an editor's status refresh) leaves the files gone and the index still carrying all three stages. | Modify/delete conflict, "Keep their version" (the deleting side) while VS Code refreshes → `indexLocked` toast, file deleted from disk, row still conflicted with no file behind it. | Collect the paths in the closure and delete the files after `index.write()` succeeds; a locked-index test for `remove_paths` (H3). | fix — files collected in the closure, deleted after `with_index` returns |
| L2 | `crates/git-core/src/commit.rs:56` (`.filter(\|l\| !l.starts_with('#'))`) | The comment character is hardcoded; git writes `MERGE_MSG`'s conflict block with `core.commentChar`. Verified with `;`: `; Conflicts:` / `;\tf.txt` survive the filter, and `git commit -F` (cleanup `whitespace`) keeps them verbatim. | `core.commentChar ";"`, conflicted merge, resolve, Commit → the message ends with `; Conflicts:` / `;\tf.txt`. | Read `core.commentChar` (default `#`; `auto` → git's choice) and filter on it. | fix — `comment_prefix` reads `core.commentChar` through `config::get` (`auto` and empty → `#`; kept a `String`, so git 2.45's multi-character values work) |
| L3 | `crates/git-core/src/cli/ops.rs:66-67` | The `Diverged` doc comment says "(the fetch already happened)", false for `merge --ff-only` from the Merge dialog, which fetches nothing. | A reader assumes a fetch ran and Pull is pointless after a plain merge. | Move the parenthetical onto the pull half. | fix |
| L4 | `src/store/commitStore.ts:403` vs `:417` | The abort branch `if (prefill && !amend && untouched(get())) set({ summary: "", body: "", prefill: null })` also wipes a `prefill` written by `useMessage` (the Message-history menu). | Merge conflicts; pick "Fix the parser" from Message history; Abort the merge → the editor is emptied. | Record who wrote the prefill (`prefill.from === "pending"`) and take back only that one. | fix — `prefill.from: "amend" | "pending" | "history"`; abort takes back `pending` only |
| L5 | `src/lib/paths.ts:35` | `.replace(/^[a-z+]+:\/\/\/?/i, "")` eats the third slash unconditionally: `file:///home/u/bare.git` → `home/u/bare` (the Windows drive case is what the tests pin). | Linux / macOS build, clone from `file:///home/me/bare.git` → the status bar's remote reads `origin · home/me/bare`. | Strip the third slash only before a drive letter; POSIX test (H10). | fix |
| L6 | `src/screens/RepoWindow/RevisionGrid/WorkingTreeRow.tsx:42` | The pseudo-row's only text is `Working tree · N changes`; `b15c109` shows it for a merge with an empty status, so the row that exists to commit the merge says "0 changes" next to a toolbar button reading "Merge to commit". | Fixture merge resolved to HEAD → "Working tree · 0 changes". | "Working tree · merge to commit" when `changes === 0` and the state is `merge` (pass `merging` from the grid). | fix — `WorkingTreeRow` takes `merging` from the grid |
| L7 | `src/screens/RepoWindow/CommitPanel/MessageColumn.tsx:174` with `:65` | Same state: the author line reads "· will commit **0** staged files" beside an enabled Commit. | A smoke tester files it. | "· will record the merge" when `stagedCount === 0 && merging`. | fix |
| L8 | `src/store/statusStore.ts:36,46,102` vs `src/store/commitStore.ts:488` | The pseudo-row exemption is merge-only while `prefillPending` fires for **every** non-clean state. `git cherry-pick <already-applied>` leaves `CHERRY_PICK_HEAD` + `MERGE_MSG` with an empty status (verified): the editor is filled, then `dropWorkingTreeIfClean` clears the selection and Commit is disabled — the message is unreachable, and the sequencer banner sends the user to a terminal anyway. A stopped rebase also writes `MERGE_MSG` (verified) though its Continue does not use the editor. | Run git command… `cherry-pick <applied commit>` → banner "finish or abort it in a terminal", editor prefilled with a message nothing can commit. | Prefill for `merge` only. | fix — merge only, in the subscription and the post-await guard |
| L9 | `src/store/commitStore.ts:393` and `:410` | The amend prefill and the pending prefill both await an IPC and then write the editor; neither guards against the other, and `untouched` is true for whatever the other just wrote, so the later resolution wins. | Mid-merge with the refs sync in flight, tick Amend; `getMergeMessage` resolves last → `MERGE_MSG` in the editor with Amend ticked → committing rewrites HEAD with the merge message. | One prefill sequence number bumped by both entry points and re-checked after the await; test (H11). | fix — module-level `prefillSeq`, bumped and re-checked by both |
| L10 | `src/components/ui/Toast/Toast.tsx:27` with `toastStore.ts:56`, `commitStore.ts:214` | The action now runs `onClose()` first. `toastError`'s Retry re-enters `run`, which returns `false` silently when another mutation holds `busy` — the toast is gone and nothing happens (the previous review's M8 shape). | "Index is locked — Retry"; start Stage all, click Retry while it runs → the error disappears, the retry never runs. | Toast "Operation in progress" when `run` refuses (or dismiss only once the action started). | fix — a refused `run` pushes an info toast "Operation in progress" (wording from `opsStore.runOp`'s own refusal) |
| L11 | `docs/smoke-cdp.md:37-39` | Names the commit-panel lists as `[role="tree"]` / `treeitem`, but the default is **flat**: `treeModeStore.ts:10` reads localStorage (false on a fresh profile) and `FilesColumn.tsx:293,392` render `listbox` / `option` then. The full-rewalk record notes the walk ended in list mode. | A scripted walk from this file gets zero matches and concludes the panel did not render. | Give both forms and say list mode is the default. | fix — doc |
| L12 | `docs/plans/2026-09-02-next-plan.md:42-43`, `:103` | "**Walk `docs/smoke-test-post-v1.md`** … nothing in it has been used in a real window" — every A–J bullet is ticked and the next bullet plus the full-rewalk record say so; step 0 of the suggested order is stale for the same reason. | Someone re-walks all ten groups. | Replace with "walked 2026-09-05/06, see the full-rewalk plan"; drop step 0. | fix — doc |
| L13 | `README.md:82-83` | "Linux and macOS are compiled there only (no rendering check…)" — Linux rendering was walked under WSLg (`2026-09-05-full-rewalk.md:150-164`); only macOS is compile-only. | A contributor concludes Linux rendering was never looked at. | Say macOS only; point Linux at the WSLg walk. | fix — doc |
| L14 | `.github/workflows/release.yml:31,42-45` | The version gate compares the tag with the root `Cargo.toml` only; `package.json`'s `version` — what the start screen renders (`StartScreen.tsx`, asserted by `smoke-test.md:57`) — is never checked. | Bump `Cargo.toml` to 0.2.0, forget `package.json`, tag → the release ships 0.2.0 whose start screen says 0.1.2. | Read `package.json`'s version in the same step and fail on a mismatch. | fix |
| L15 | `docs/smoke-test.md:3-8` | The opening paragraph still says "Nothing in this app has been exercised in a real window … This checklist is the acceptance gate. Run it once end to end before calling v1 done", contradicting `:10-14` seven lines below. | A first-time reader treats every tick as unverified. | Rewrite `:3-8` as the doc's current purpose (regression checklist, ticks are the record); keep the acceptance history to a sentence. | fix — doc |
| L16 | `README.md:81` | "walkthrough was completed end to end on 2026-09-01" while three steps are unticked (Resolve in editor ×2, DPI) and by the doc's own rule unwalked. | A reader concludes the v1 checklist has no open items. | "…apart from the three steps this machine cannot reach". | fix — doc |
| L17 | `src/README.md:145` | `runGit`'s "no toast … unless conflicts / auth / non-fast-forward" misses `diverged` (`opsStore.ts:153`). | A reader concludes a typed `git pull --ff-only` on diverged history stays silent. | Add `/ diverged`. | fix — doc |
| L18 | `src/screens/RepoWindow/DiffViewer/DiffViewer.tsx:146,148`; `lineSelection.ts:32,45` | The Shift clamp compares the next pick's hunk with the **cursor's**, but `clickLine`'s range needs `sel.anchor.hunk === ref.hunk`. Plain ↑/↓ cross hunks leaving the anchor behind, so the clamp passes and `clickLine` falls to `:45`, replacing the selection with one line. Reproduced. | Select two lines in hunk 1 with Shift+↓, ↓ twice into hunk 2 ("2 lines selected"), Shift+↓ → the selection collapses to one line in hunk 2. | Also clamp when `sel.anchor` is in another hunk than the cursor (or re-seed the anchor at the cursor); test (H16). | fix — the **re-seed** option: the anchor is re-seeded at the cursor when it sits in another hunk, so Shift+↓ after crossing starts a fresh 2-line range there |
| L19 | `DiffViewer.tsx:92` (`prev.path === diff.path`) | The carry-over guard is the path only. Selecting the same file in the *other* list produces a different diff at the same path, and the cursor index (and a content-matching hunk's selection) is carried into it. Reproduced. | `a.rs` in both lists: cursor on the last line of its unstaged diff, click `a.rs` in Staged, Tab into the diff → the cursor is mid-diff. | Include the list (`actions?.target`) in the guard; test (H17). | fix — `loadedTarget` ref beside `loaded`; the guard also compares `actions?.target` |
| L20 | `src/screens/RepoWindow/CommitPanel/FilesColumn.tsx:135` (`useEffect(() => setMenu(null), [entries])`) | Closes the file menu on `entries`' *identity*, which changes on every status refresh even when nothing changed (see M5). Reproduced with a byte-identical status. | Right-click a file; the 100 ms debounced refresh after a background save fires → the menu vanishes under the pointer. | Compare a signature of paths + statuses, not identity; tests both ways (H22). | fix — keyed on a `path + status` signature of the entries |
| L21 | `src/store/statusStore.ts:46` vs `Toolbar.tsx:253` and `banners.ts:51` | `useShowWorkingTree` suppresses the pseudo-row while a text filter flattens the walk, but the toolbar's `merging` and the banner's Commit merge do not know `flat`; `wtSelected && showWt` never mounts the panel, so both are dead clicks (pre-existing for a dirty tree; new for the merge case). | Mid-merge, type in "Search commits", click Commit or Commit merge → nothing. | `openCommitPanel()` clears the filter before `selectWorkingTree()`; test (H19). | fix — `openCommitPanel()` restarts the walk with `text: null` when `log.flat` before `selectWorkingTree()`; the toolbar's Commit routes through it and the search field follows the store's `filter.text` |
| L22 | `WorkingTreeRow.tsx:29` (`aria-rowindex={1}`) vs `RevisionGrid.tsx:130,191` | The sticky header is a `role="row"` with no `aria-rowindex`, implicitly row 1 — the index the pseudo-row claims; `aria-rowcount` excludes the header. | A screen reader announces "row 1 of 4" for the working-tree row, the same coordinates as the header. | Header `aria-rowindex={1}`, data rows +1, `aria-rowcount={count + 1}`. | fix — header `aria-rowindex={1}`, pseudo-row 2, data rows +2, `aria-rowcount={count + 1}`; the grid tests select rows inside `[role="rowgroup"]` |
| L23 | `FileContextMenu.tsx:81` and `FilesColumn.tsx:265` | Discard is hidden / refused for the whole selection as soon as one file is conflicted, silently; the Stage item above keeps working and says "(N skipped)". The Delete path returns before `preventDefault`. | Ctrl-select two modified files and a conflicted one, Delete → nothing, no toast; right-click → no Discard item. | Mirror Stage: act on the non-conflicted paths with the same "(N skipped)" title; disable only when all are conflicted. | fix — Discard always renders for the unstaged list, acts on the non-conflicted paths with a "(N skipped)" title, disabled only when none is left; Delete follows the same rule |
| L24 | `FilesColumn.tsx:151` with `commitStore.ts:286-289` | When the last unstaged file is staged, `syncWithStatus` hands the selection to the staged list, but the `held` repair refocuses the now-empty **unstaged** list, whose `onKeyDown` returns at once. Reproduced. | Ctrl+A, Enter in Unstaged → all staged; Enter again → nothing; the ring sits on "No unstaged changes". | Skip the refocus when `entries.length === 0` and let the list matching `commitStore.list` take it; test (H18). | fix — an emptied list releases `held` and focuses the sibling list of the same mount (`siblingList` walks up to the nearest ancestor holding the other `[aria-label="… files"]`, so the panel and the commit dialog each hand off within their own pair); only a list that just emptied hands off — one focused while already empty keeps the focus (caught reviewing the fix) |
| L25 | `src/components/ui/CommandInput/CommandInput.tsx:39` reached from `RunCommandDialog.tsx:46` (`placement="up"`) | With `placement="up"` the list is clamped to `y = 4` when there is no `h + 8` px above the field, then extends down **over** it; the dialog is centred in a window whose minimum height (600) does not give 328 px of headroom. | Window at minimum height, Ctrl+Shift+G, type `che` → the list covers the input. | Pick the side with room and cap `h` to it. | fix — `useListPosition` measures the room above and below, flips to the side that fits and caps the list's `maxHeight` to it |

## P — performance

| # | Where | What | Scenario | Proposed fix | Decision |
|---|---|---|---|---|---|
| P1 | `crates/git-core/src/refs.rs:291-305` (`children_first`) | The pick is **cubic** in the run length, not quadratic as the comment says: `position` scans up to `n` candidates, each an `n`-element `any` over `parent_ids`, once per pop. Runs from a real rebase are ~70 commits (harmless); a history with a **pinned** committer date (`fast-import`, a mirror script, `GIT_COMMITTER_DATE`) makes the run the whole walked range. | A repo imported with one committer timestamp, n ≈ 2000 → ~10^10 parent comparisons per refs snapshot. | Kahn's algorithm over an in-run child-count map (O(n + e)); fix the comment; unit test (H4). | fix — Kahn's over an in-run child-count map, ready queue seeded in walk order (deterministic, `tests/merged.rs`'s pinned order holds); leftovers appended defensively |
| P2 | `DiffViewer/lineSelection.ts:75,84` (`hunkMap` in `carryRef` and `carrySelection`), called back-to-back from `DiffViewer.tsx:93,95` | `hunkMap` joins every hunk's text of both diffs into id strings, and the `[diff]` effect calls it twice per reload with the same arguments; every hunk / line stage reloads. | Staging hunks one by one in a 20 000-line diff builds ~4× the diff's text as strings per click, on the main thread. | Compute `hunkMap(prev, diff)` once in the effect and pass it to both. | fix — `hunkMap` exported and computed once; `carryRef` / `carrySelection` take the map |

## H — hygiene / tests

| # | Where | What | Proposed fix | Decision |
|---|---|---|---|---|
| H1 | `crates/git-core/src/commit.rs:15` (`allow_empty`) vs its only caller `commands/stage.rs:467` (`false`) | Dead flexibility: no caller passes `true`; only `commit_args_shapes` exercises it. The one state needing it (cherry-pick resolved to HEAD) is reachable only through Run git command…. | Drop the parameter and its arm. | fix — `commit_args(message_file, amend, signoff)` |
| H2 | `commit.rs:49`; `commands/stage.rs:502` | Doc comments say "merge / cherry-pick / revert"; a conflicted **rebase** writes `MERGE_MSG` too (verified), and the frontend calls the reader for every non-clean state. | Add rebase to both (moot for the prefill once L8 lands, still true of the file). | fix |
| H3 | `stage.rs:198` (`remove_paths`) | Both new rollback tests cover `stage_paths` only; `remove_paths` — the one with a working-tree side effect (L1) — has none. | Locked-index case asserting the stages survive and, with L1, the file too. | fix |
| H4 | `refs.rs:294` (`children_first`) | No direct test; `tests/merged.rs:204` reaches the ordering through libgit2's heap order and pins one order, not the rule. | Unit test: parent-first same-second `Vec` in, children-first out, including a merge with both parents in the run. | fix |
| H5 | `README.md:80` | "158 Rust and 367 frontend tests" — actual 162 / 394; the same row rotted in the previous review (L34). Found by two passes. | Drop the counts; cite the gates. | fix — doc |
| H6 | `src/components/ui/Toast/` | No `Toast.test.tsx`: the action-then-close order and the `alert` / `status` split are pinned nowhere. | One test file: `onClose` then `onClick`; error → `alert`, info → `status`. | fix |
| H7 | `src/components/ui/Dialog/Dialog.test.tsx:85` | The busy-recovery test asserts only that the dialog contains the focus — which is why M2 passes. | Assert the focused element is the `Name` textbox. | fix — with M2 |
| H8 | `src/lib/paths.test.ts:41-42` | `prettyUrl` covered for `file:///C:/…` and `file://server/…` only. | Add the POSIX `file:///home/u/bare.git` case. | fix — with L5 |
| H9 | `src/store/commitStore.test.ts:445-497` | `prefillPending` block lacks a `useMessage` prefill surviving an abort (L4) and a state change during an in-flight `setAmend` (L9). | Add both beside the fixes. | fix |
| H10 | `src/store/opsStore.test.ts:150-155` | The `diverged` case never passes `quietFailure`, so the `loud`-list entry (the reason a typed `git pull --ff-only` still toasts) is untested; dropping it keeps the suite green. Found by two passes. | A `{ quietFailure: true }` case asserting the toast. | fix |
| H11 | `src/store/statusStore.ts:36` | `useMerging` lives in the status store but reads only `repoStore`; Toolbar and MessageColumn import a repo-state hook from the status module. | Move it next to `selectSelectedOid` in `repoStore` (or re-export). | fix — `useMerging` lives in `repoStore` next to `selectSelectedOid` |
| H12 | `.github/workflows/release.yml:120` | `cargo binstall --no-confirm 'tauri-cli@^2'` floats, directly under a comment explaining why the action above is pinned; nothing in CI runs the Tauri CLI, so a 2.x change surfaces first as three red legs on a tag. | Pin an exact `tauri-cli@2.x.y`, bumped deliberately. | fix — `tauri-cli@2.11.4`, matching `@tauri-apps/cli` in `package-lock.json` |
| H13 | `release.yml:19-24` (`version`), `:62-65` (`build`) | `c23992d` removed the workflow-level `contents: write` and gave `publish` its own, but `version` and `build` have no `permissions:` block and take the repository default; `build` runs third-party binaries. | `permissions: contents: read` on both. | fix |
| H14 | `release.yml:58-60` | `checks:` has the tag guard but no `needs: version`, while `build` does: a mismatched tag fails `version` in seconds and the three-OS checks still run guarding a `publish` that cannot run. | `needs: version`. | fix |
| H15 | `src/store/statusStore.test.ts:1,61,207` | `renderHook` is used with no `afterEach(cleanup)`; vitest runs without `globals`, so RTL never auto-cleans; every other file declares it. Harmless while it is the last test. | Add `afterEach(cleanup)`. | fix |
| H16 | `src/store/statusStore.test.ts:202-209` | The only `useShowWorkingTree` test runs with `flat: false`, so the `&& !flat` term is unpinned against the new `merging` branch (L21's case), and no sibling state (rebase / cherry-pick + empty status) is pinned either way. Found by two passes. | `flat: true` + merging → false; `state: "rebase"` + empty status → false. | fix |
| H17 | `docs/plans/2026-09-05-full-rewalk.md:118` | The group I transcript reads `&& git push origin refs/tags/v-pushed` without `--progress`; the preview is built from the real argv (`gitArgs.ts:25-27`), and both checklists say `--progress`. | Correct the transcript line. | fix — doc |
| H18 | `src/screens/RepoWindow/CommitPanel/CommitPanel.test.tsx:177-190` | The Enter-staging refocus test never empties the list — the case the fix's own comment names, which only works because the empty state renders inside the same `scrollRef` element. | A case that stages every row and asserts the focus (per L24: on the list that owns the selection). | fix — with L24 |
| H19 | `CommitPanel.test.tsx:174-188`; `DiffViewer.test.tsx` | The only `held` tests drive the good path; nothing pins the release side (blur to `<body>` then an unrelated refresh; blur to an outside element then a refresh). | Add both cases in both components. | fix — with M5 |
| H20 | `DiffViewer.test.tsx:212-241` | The Shift clamp test keeps anchor and cursor in one hunk (cannot see L18) and never covers Shift with `sel.anchor === null` in a later hunk. | "Plain ↓ across the edge, then Shift+↓" asserting the selection is kept or extended. | fix — with L18 |
| H21 | `DiffViewer.test.tsx:291-313` | Every carry-over test re-renders with the same `actions.target` (L19 unpinned), and none covers `carryRef` returning null for the cursor's hunk while `picks` shrinks below `cursor`. | A `target` switch asserting cursor 0 and an empty selection; a shrink case. | fix — with L19 |
| H22 | `RevisionGrid/commitMenu.test.ts:86-89`; `RevisionGrid.test.tsx` | `commitBranchActions` has an unborn-HEAD unit test but no component test asserts what the menu renders — why M4 survived L19. | Render-level: unborn → no item starting with "Merge"; HEAD's own commit → no merge, no rebase. | fix — with M4 |
| H23 | `CommitPanel.test.tsx:377-385` | The empty-merge test asserts only Commit's `disabled`; the author line (L7), the empty-state strings, Stage all / Unstage all disabled, Commit & Push are unasserted. | Extend to the panel's text in that state. | fix — with L7 |
| H24 | `src/screens/RepoWindow/dialogs/` | No `RunCommandDialog.test.tsx`; `placement="up"` is covered only by `CommandInput`'s tests, which never exercise geometry (jsdom rects are zero). | Assert `placement` reaches `CommandInput`; geometry stays a CDP step. | fix — `RunCommandDialog.test.tsx` mocks `CommandInput` and asserts `placement === "up"` |
| H25 | `FilesColumn.tsx:135`; `CommitPanel.test.tsx` | The previous review's M7 ("close the menu when the entries change") has no test, so L20 is unguarded either way. | Identical status → stays; a path removed → closes. | fix — with L20 |
| H26 | `docs/plans/2026-09-02-next-plan.md:23,41,72,97` | Section order is A, B, D, C. | Move §C above §D. | fix — doc |

## Unverified

- `Menu.tsx:62-64` / `Dialog.tsx:55-57` `lost` heuristic vs the browser's focus-on-mousedown: when a
  menu is dismissed by pressing the mouse on an *outside focusable* control, whether
  `document.activeElement` has moved by the time the passive cleanup runs is timing-dependent; if
  not, the guard pulls the focus back to the trigger. **Walked 2026-09-06 over CDP: fine** — a
  mousedown on the search field or on a toolbar button while a row menu is open lands the focus on
  that control and closes the menu, no pull-back (`docs/plans/2026-09-06-group-k-walk.md`).
- `DockPanel` (`RepoWindow.tsx:101-110`): whether `expand()` after a user resize restores the
  dragged height or lands on `minSize` is internal to `react-resizable-panels`; not read. **Walked
  2026-09-06: it lands on the 160 px minimum** — finding 1 of the group K walk, fixed there: the
  panel remembers its last open height through `onResize` and reopens with `resize(last)`.
- `RevisionGrid.tsx:93` computes the Shift+F10 menu's `y` from `rowH` and `scrollTop`, correct for
  uniform rows; not checked against a `--row-h` override. **Moot**: `--row-h` is set once, in
  `tokens.css` (26px), and overridden nowhere.
- `tests/merged.rs:200-231` (`a_chain_stamped_in_one_second_is_still_merged`): not confirmed to fail
  without `children_first` (needs the pre-`7b6ef75` `refs.rs`); the fixture is the right shape.
- `release.yml:182` writes the `.sha256` sidecars with no trailing newline; `sha256sum -c` and
  `shasum -c` accept that in practice, not tested against the published assets.
- `docs/smoke-fixtures.ps1` was checked line by line against the checklists, not re-run.

## Checked and fine (coverage record)

- **`git commit -F` with an empty index during a merge** (b15c109's backend assumption): verified,
  exit 0, two-parent merge recorded, `MERGE_MSG` removed by that commit.
- **`reachers`' run grouping is complete**: libgit2's `GIT_SORT_TIME` walk is a max-heap on committer
  seconds and enqueues parents when a commit is popped, so a same-second parent is in the queue while
  its child's run drains; `children_first` is a correct topological order (children before parents),
  the one hole — a commit stamped newer than its child — is stated in the doc comment.
- **`with_index`** does what it says (`index.read(true)` forces the on-disk index back); both new
  tests reproduce the hazards they name. **No stale libgit2 index after a CLI write**: every entry
  point `read(false)`s first, `status()` sets no `NO_REFRESH`, and the NULL-index diffs re-read.
- **No mutex held across an await** in `commands/stage.rs`; `resolve_conflict` and `commit` drop the
  guard around each CLI run. **Error mapping** unchanged: `GitOutput::check` → `Cli`, held
  `index.lock` → `IndexLocked`, unreadable `MERGE_MSG` → `None`.
- **`Diverged`** splits cleanly from `NonFastForward` (`NON_FF_PATTERNS` first; the integration test
  passes); serde tag matches `types.ts`; `opsStore` gives it its own toast without Pull and lists it
  as loud. **`MERGE_MSG` lifecycle**: removed by commit, `merge --abort`, `cherry-pick --abort` and
  a completed `rebase --continue`; never written by a fast-forward.
- `checkout_detach` is fully gone (the gap it leaves is M1); `tauri.conf.json`'s dropped `version` is
  deliberate and the workflow reads the workspace version, which `src-tauri` inherits.
- `loadDiff` / `loadStats` / `run` sequence guards and `diffContext` travelling with the diff;
  `dropWorkingTreeIfClean` is order-independent across `runOp`'s concurrent `refresh()` /
  `syncRefs()`; `syncRefs` coalescing; `prefillPending`'s post-await guards and the subscription
  order on a repo switch; `useShowWorkingTree`'s `flat` clause and the grid's index math agree.
- `splitArgs` drops the trailing space `CommandInput.accept` appends; `capRecents` / `touch` /
  `remove` / `togglePin` funnel through one `update`; `ipc.ts` wrappers all go through `call`;
  the conflicts toast becoming `info` is a recorded decision; `Menu`'s `useRestoreFocus` runs before
  `useMenuDismiss`.
- `carrySelection` / `carryRef` matching is content-keyed and order-preserving; `DiffViewer`'s effect
  ordering matches its comments; `hunkActions` never resurrects Discard; `ContextMenu` restores the
  opener's focus before any `held` repair; `.menuBranch` ellipsizes each half; the merge and rebase
  labels name the same branch; `RevisionGrid.onKeyDown` index math with the pseudo-row; the status
  bar's remote pick; `StartScreen.onListKey`'s input-vs-list split; `FileContextMenu` / `FilesColumn`
  agree on the one-vs-many stage rule.
- `release.yml`'s `checks` is the same reusable workflow CI calls, at the tag's commit, on the same
  matrix; `ci.yml`'s `paths-ignore` cannot let a tagged commit through unchecked; the version gate's
  `grep -m1` hits the workspace line; the staging rewrite lands the ten assets where `publish`
  expects them and `publish` alone has `contents: write`.
- `docs/smoke-cdp.md`'s other selectors, `smoke-dialog.ps1`'s internals, `smoke-fixtures.ps1`'s
  output shape, `src/README.md`'s store map, `README.md`'s shortcut table, and the tick state agreed
  between the two checklists and the full-rewalk record — all as documented, with the exceptions in
  the rows above.
- New tests confirmed non-vacuous by counterfactual: the Shift clamp, the Discard-buttons pair, the
  start-screen mid-text Delete, the merge/HEAD selection test's refs half, `carrySelection` /
  `carryRef`; test isolation (`refs: null` resets) holds.
